import React from "react";
import styles from "./style.module.css";

const DailyStreaksWidget = () => {
  return (
    <div className={`card ${styles.dailyStreaksWidget}`}>
      DailyStreaksWidget
    </div>
  );
};

export default DailyStreaksWidget;
