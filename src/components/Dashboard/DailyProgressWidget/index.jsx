import React from "react";
import styles from "./style.module.css";

const DailyProgressWidget = () => {
  return (
    <div className={`card ${styles.dailyProgressWidget}`}>
      DailyProgressWidget
    </div>
  );
};

export default DailyProgressWidget;
