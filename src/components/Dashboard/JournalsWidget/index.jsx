import React from "react";
import styles from "./style.module.css";

const JournalsWidget = () => {
  return <div className={`card ${styles.journalsWidget}`}>JournalsWidget</div>;
};

export default JournalsWidget;
